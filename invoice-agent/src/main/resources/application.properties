# Agent Configuration
invoice-agent.agent.app-name=InvoiceAgent
invoice-agent.agent.user-id=customer1234
invoice-agent.agent.model-name=gemini-2.5-flash

# Google Cloud Storage Configuration
invoice-agent.gcs.project-id=${GOOGLE_CLOUD_PROJECT}
invoice-agent.gcs.bucket-name=seroter-roofing-invoices

# MCP Server URL from environment variable, with a default for local dev
mcp.toolbox.url=${MCP_TOOLBOX_URL:http://localhost:8081/mcp/tool/sse}